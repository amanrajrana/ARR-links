extends base.pug

block include-file
    style(type="text/css").
        table {
            margin: auto
        }

        table th, table td {
            border: 1px solid grey;
            padding: 5px 10px;
        }

block content
    h1 Hey this is me
    table
        tbody  
            tr
                th SNo.
                th shortURL 
                th LongURL
                th Total click
            
            -
                let i = 1;
            each url in allUrl
                //- -
                    let domain = 
                tr 
                    td= i+'.'   
                    -i++
                        
                    td
                        a(href= domain+url.shortId)= domain+url.shortId
                    td 
                        a(href= url.redirectURL)= url.redirectURL 
                    td= url.visitHistory.length

